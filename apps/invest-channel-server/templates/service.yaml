apiVersion: v1
kind: Service
metadata:
  name: {{ include "invest-channel-server.fullname" . }}
  labels:
    {{- include "invest-channel-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "invest-channel-server.selectorLabels" . | nindent 4 }}
